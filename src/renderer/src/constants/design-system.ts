/**
 * レンダラープロセス専用のデザインシステム定義。
 * 将来的な拡張を考慮し、icons, colors 等のオブジェクトで構造化しています。
 */
export const UI_TOKENS = {
  icons: {
    /** 標準的なアイコンのサイズ (px) */
    size: 16,
    /** バッジの削除ボタンなどの小さなアイコンサイズ (px) */
    sizeSmall: 12,
    /** インスペクターなどの強調表示用のアイコンサイズ (px) */
    sizeLarge: 48,
    /** Lucideアイコンなどの標準的な線の太さ */
    strokeWidth: 1.5,
    /** 小さなアイコンでも視認性を確保するための太めの線 */
    strokeBold: 2.5,
    /** ツールバーのロゴなどに使用するアイコンサイズ (px) */
    logoSize: 22
  }
} as const;
